# plot a LatticeMap

Calls a standard plot.stars, but flips the first argument around the x
axis

## Usage

``` r
# S3 method for class 'LatticeMap'
plot(x, ...)
```

## Arguments

- x:

  object of class `LatticeMap`

- ...:

  other parameters passed to `stars[]`
